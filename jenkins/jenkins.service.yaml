apiVersion: v1
kind: Service
metadata:
  name: jenkins
  labels:
    app: jenkins
spec:
  type: LoadBalancer
  ports:
    - name: jenkins
      port: 50000
    - name: http
      targetPort: 8080
      port: 80
  selector:
    app.kubernetes.io/name: jenkins
