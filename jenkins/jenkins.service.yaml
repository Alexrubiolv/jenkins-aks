apiVersion: v1
kind: Service
metadata:
  name: jenkins
  labels:
    app: jenkins
spec:
  type: LoadBalancer
  ports:
    - name: jenkins
      port: 50000
    - name: http
      targetPort: 9080
      port: 80
  selector:
    app.kubernetes.io/name: jenkins
